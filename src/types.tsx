export type Job = {
  companyName: string;
  title: string;
  location: string;
  dates: string;
  bulletPoints: string[];
};

export type School = {
  name: string;
  info: string;
  location: string;
  dates: string;
};

export type SideProject = {
  name: string;
  bulletPoints: string[];
};

export type Skill = {
  name: string;
  skills: string[];
};
